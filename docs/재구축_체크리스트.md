# 재구축 체크리스트 — destroy 이후 인프라 재구성 절차

> **분리 계기(2026-08-13):** `teardown_체크리스트.md`가 destroy와 재구축을 한 파일에 담고 있었다. 파일 이름과 실제 내용 범위가 달라져서, 옛 8~9장(재구축 관련)을 이 문서로 옮겼다.
> destroy 절차는 `teardown_체크리스트.md`에 있다. 게이트①~⑤(살릴지 버릴지·삭제 경로 등)도 그 문서 §6에 있다 — 그중 게이트④(ArgoCD 설치 주체)가 이 문서에서 실제로 쓰인다.
> 장·절 번호(8·8-1~8-4·9)는 원본 문서 번호를 그대로 유지했다. 팀 논의·PR·규약서에서 이미 그 번호로 참조된 곳들과의 연결을 끊지 않기 위해서다.

---

## 8. 실전 재구축 절차 요약 — 배포팀 5줄 + hailcast-rds-secret 자동화 갱신

destroy 이후 Terraform으로 다시 세운 리소스는 이름은 같아도 ARN 등 내부 ID가 새로 발급된다. 매니페스트에 그 ID를 문자 그대로 박아둔 곳만 아래 8-1의 새 값으로 바꾸면 된다. 이름으로 정해지는 값(IRSA ARN·SQS URL·ECR 주소 등)은 재구축해도 그대로다.

### 8-1. 재구축 때 값이 바뀌는 것과 안 바뀌는 것 (2026-08-18 재구축 실측 반영)

destroy 이후 Terraform으로 다시 세운 리소스는 이름은 같아도 ARN·ID 같은 내부 식별자가 새로 발급된다. 그중 매니페스트에 그 값을 문자 그대로 박아둔 곳만 사람이 고치면 된다 — 아래 첫 표다. 나머지는 값 자체는 바뀌어도 Terraform이 apply 안에서 자기 리소스끼리 다시 배선하거나(IRSA 신뢰정책 등), 매니페스트가 이름으로만 참조해서 사람이 손댈 자리가 없다.

**사람이 고쳐야 하는 값 (배포팀 5줄, 인프라가 값을 전달)**

| 값 | 8/18 재구축 실측값 | 받는 곳(manifests) | 안 고치면 |
| --- | --- | --- | --- |
| ACM 인증서 ARN (`alb_certificate_arn`) | `arn:aws:acm:ap-northeast-2:797452357900:certificate/ac119b3a-0135-4ce2-b6d3-024f9d2acda2` | `apps/call-api/ingress.yaml:19` · `apps/frontend/ingress.yaml:15` · `apps/predict/ingress.yaml:11` | 없는 인증서 지목 → HTTPS 리스너 안 붙음 |
| VPC ID (`vpc_id`) | `vpc-03fe7938497b3ed0c` | `addons/aws-load-balancer-controller/values.yaml:3`의 `vpcId` | 컨트롤러가 없는 VPC 지목 → ALB 자체가 안 생김 |
| RDS 마스터 시크릿 ARN (`rds_master_secret_arn`) | `arn:aws:secretsmanager:ap-northeast-2:797452357900:secret:rds!db-1530d81c-e164-48df-83d2-06f4d952552e-sVe4XH` | `platform/external-secrets/externalsecret-rds-credentials.yaml:19,23` | ESO가 없는 시크릿을 조회해 계속 실패 → placeholder Secret은 생기지만 `DB_USER`·`DB_PASSWORD` 키가 비어 `secretKeyRef`가 못 찾음 → call-api·predict·worker 3개가 `CreateContainerConfigError`로 기동 실패 |
| 모델 아티팩트 버킷 이름 (`model_bucket_name`) | `hailcast-dev-model-artifacts-58b4f8fd` | `apps/predict·call-api·simulator·weather-cron/deployment.yaml` 4곳 + 콘솔 수동 IAM 정책 `hailcast-dev-model-upload-manual`(infra 규약서 §5-9) | `NoSuchBucket` / 모델 업로드가 `AccessDenied`로 막힘(앱은 이 에러를 "파일 없음"으로 표시) |
| ALB 인바운드 제한 SG ID (`eks_alb_cloudfront_sg_id`) | `sg-02050c3eb49f83935` | `apps/predict·call-api·frontend/ingress.yaml`의 `security-groups` 어노테이션 | 옛 SG는 재구축 때 같이 지워지므로, 옛 값을 그대로 두면 존재하지 않는 SG를 지목해 Ingress 반영이 실패한다 |

**바뀌지만 손댈 필요 없는 값** — Terraform이 apply 안에서 자동 재배선

EKS 클러스터 엔드포인트(오늘: `https://5AC483248A874477664C82668289E076.gr7.ap-northeast-2.eks.amazonaws.com`) · OIDC Provider ARN · EKS 클러스터/노드 보안그룹 ID · RDS 엔드포인트(오늘: `hailcast-dev-rds-postgres.cneioc4uwo5n.ap-northeast-2.rds.amazonaws.com:5432`) · CloudFront 도메인(오늘: `d84z8mzt6y899.cloudfront.net`) · 서브넷 ID. 매니페스트 어디에도 이 값들이 하드코딩돼 있지 않다.

**안 바뀌는 값** — 이름으로 정해져 재구축해도 그대로

| 값 | 8/18 재구축 실측값(참고) |
| --- | --- |
| EKS 클러스터 이름 | `hailcast-dev-eks` |
| IRSA 역할 ARN 10곳 | 예: `arn:aws:iam::797452357900:role/hailcast-dev-irsa-predict` (check-contract 10/10 확인) |
| SQS 콜 큐 URL | `https://sqs.ap-northeast-2.amazonaws.com/797452357900/hailcast-dev-call-queue` |
| Karpenter 중단 큐 이름 | `hailcast-dev` |
| ECR 레포 주소 | `797452357900.dkr.ecr.ap-northeast-2.amazonaws.com/hailcast-dev-<서비스>` |
| Ingress host (`origin_domain_name`) | `origin.hailcast.myminiinfra.store` |
| 서비스 URL | `https://hailcast.myminiinfra.store` |
| DynamoDB 오답노트 테이블 | `hailcast-dev-prediction-log` |
| CUR 정의 이름 | `hailcast-dev-cur` (이름은 고정, 대상 버킷 이름만 바뀐다 — §8-4) |
| EC2NodeClass 셀렉터 · 네임스페이스·ServiceAccount·ScaledObject 이름 | 규약서 §5-3·§8-1 그대로 |

### 8-2. 재구축 순서 뼈대
```
1) infra: alb_dns_name=""로 리셋 → 첫 apply(로컬·사람이 직접 — CI가 먼저 하면 사람이 kubectl 못 씀)
2) infra: output 4종을 배포팀에 전달, kubeconfig 재발급
3) 배포팀: 부트스트랩 — `make bootstrap-all` (담당 조용빈님)
   ArgoCD 설치 + ESO CRD 설치 + rds-secret force-sync 를 한 번에 한다.
   8/18 재구축에서 손으로 하던 세 가지를 스크립트로 흡수했다(manifests #81).
   실패하면 스크립트가 복구 명령을 안내한다 — 그대로 따르면 된다.
   상세: ①은 8-5, ②③이 실패했을 때는 8-3-1·8-3-2
4) 배포팀: 위 8-1 5줄 반영 + PR + 머지 → Application 전부 Synced/Healthy 확인
5) 앱팀: 이미지 6종 재빌드(dev push 아니라 workflow_dispatch로 — push면 바뀐 서비스만 빌드돼 나머지가 ImagePullBackOff)
6) 인프라: IAM 콘솔 수동 정책의 버킷 접미사 교체 (앱팀 모델 업로드보다 먼저)
7) 앱팀: model.pkl·metadata.json 재업로드
8) 배포팀: Ingress 올림 → 인프라: alb_dns_name에 새 ALB 주소 넣어 2차 apply(CloudFront 생성)
9) 인프라: CUR 버킷 이관 + 정의 재생성 (8-4 참고. 급하지 않음 — 위 단계들과 순서 안 겹침)
10) 완료 확인: 노드 Ready·파드 Running·Application 전부 Healthy·plan No changes·서비스 URL 200·ECR 6개 저장소 전부 이미지 존재(aws ecr describe-images --repository-name hailcast-dev-<service> --region ap-northeast-2로 확인 — 5번을 건너뛰고 일반 push만 했으면 그때 바뀐 서비스 외엔 여전히 빈 저장소로 남아 ImagePullBackOff, 2026-08-18 실측: frontend)
```

### 8-3. `hailcast-rds-secret` — #58 머지로 절차가 바뀜 (조용빈님 지적 반영)

> **정상 경로에서는 `make bootstrap-all`(8-2 3번)이 아래 ①②를 대신 한다.**
> 이 절은 스크립트가 한 일을 확인하거나, 실패했을 때 손으로 처리할 때 본다.

기존 런북(2026-07-28 오전 기준) 서술:
> "자동으로 안 생긴다. 반드시 둘 다 해야 한다 — ① ESO CRD 설치, ② `kubectl create secret`으로 수동 생성"

변경된 사실(같은 날 오후 #58 머지 — `feature/rds-secret-placeholder`):
- `manifests platform/external-secrets/secret-rds-placeholder.yaml`이 sync-wave `-1`로 들어와, ExternalSecret(기본 wave 0)보다 먼저 빈 Secret을 자동 생성한다.
- ExternalSecret 2개(`creationPolicy: Merge`)가 그 빈 Secret에 `DB_USER`·`DB_PASSWORD`·`DB_HOST` 키를 자동으로 채워 넣는다.

그래서 절차가 이렇게 바뀐다:

| 단계 | 기존(오전) | 갱신(#58 반영) |
| --- | --- | --- |
| ① ESO CRD 설치 | 필요 | 여전히 필요(자동 경로에서도 CRD 없으면 ExternalSecret 리소스 자체가 안 만들어짐). 주소 확정(2026-08-13): manifests `argocd/applications/external-secrets-app.yaml`의 `targetRevision`(현재 2.5.0)과 같은 태그의 CRD 번들. 적용 전 이 값이 그대로인지 재대조.<br>`kubectl apply --server-side -f https://raw.githubusercontent.com/external-secrets/external-secrets/v2.5.0/deploy/crds/bundle.yaml` |
| ② 수동 `kubectl create secret` | 필요 | 원칙적으로 불필요할 가능성 높음 — 다만 실제로 자동 채워지는지는 미검증(이 자동 경로가 빈 클러스터에서 검증된 적이 없고, 8/3 재구축이 첫 실행) |

8/3 리허설에서 반드시 확인:
```bash
kubectl -n argocd get application platform-secrets            # Synced인지 먼저
kubectl -n hailcast get secret hailcast-rds-secret -o jsonpath='{.data}' | jq 'keys'
# 기대값: ["DB_HOST","DB_PASSWORD","DB_USER"]  ← 이 셋이 다 나오면 수동 생성 생략
kubectl -n hailcast get externalsecret                         # rds-credentials·rds-endpoint 둘 다 SecretSynced
```
키가 비어 있거나 SecretSynced가 아닐 때만 아래 수동 patch를 백업으로 실행(infra `docs/비용관리.md`의 "재구축 시 hailcast-rds-secret" 절 참조, `create`가 아니라 `patch`인 이유는 빈 Secret이 이미 있기 때문):
```bash
RDS_SECRET="$(terraform -chdir=envs/dev output -raw rds_master_secret_arn)"
CRED="$(aws secretsmanager get-secret-value --secret-id "$RDS_SECRET" --query SecretString --output text)"
kubectl -n hailcast create secret generic hailcast-rds-secret \
  --from-literal=DB_HOST="$(aws ssm get-parameter --name /hailcast/dev/rds/endpoint --query Parameter.Value --output text)" \
  --from-literal=DB_USER="$(echo "$CRED" | jq -r .username)" \
  --from-literal=DB_PASSWORD="$(echo "$CRED" | jq -r .password)" \
  --dry-run=client -o yaml | kubectl apply -f -
```

조용빈님 말대로 자동 생성이 실패해도 수동 백업 절차가 그대로 유효해서 파드가 못 뜨는 상황 자체는 안 생긴다. 문제는 이제 불필요할 수도 있는 수동 명령을 팀원이 기계적으로 실행하는 비효율과, 런북이 최신 상태를 못 따라가는 패턴이 반복된 것이다. 이 문서를 갱신할 때마다 재검증한다.

#### 8-3-1. [수동 복구] `SecretSynced: True`인데 특정 키만 비어 있을 때 (2026-08-18 실측)

위 확인 명령에서 `Ready: True`·`SecretSynced`인데도 `hailcast-rds-secret`의
`.data`에 키가 하나 빠져 있을 수 있다(실측 사례: `DB_HOST`만 누락). 원인은
ESO 컨트롤러가 막 재시작한 직후 `ClusterSecretStore`가 아직 준비되기
전이라 첫 reconcile이 `error processing spec.data[0] ... is not ready`로
실패했고, `refreshInterval: 1h` 안에는 자동 재시도가 없었던 것.

1시간 기다리지 말고 강제로 재동기화한다:
```bash
kubectl -n hailcast annotate externalsecret rds-endpoint force-sync=$(date +%s) --overwrite
kubectl -n hailcast annotate externalsecret rds-credentials force-sync=$(date +%s) --overwrite
sleep 5
kubectl -n hailcast get secret hailcast-rds-secret -o jsonpath='{.data}' | jq 'keys'
```
채워진 뒤에는 **이미 떠 있던 파드를 재시작**해야 새 값을 읽는다(Secret
갱신이 실행 중인 컨테이너의 env에 자동 반영되지 않음):
```bash
kubectl -n hailcast rollout restart deployment predict hailcast-worker
```

#### 8-3-2. [수동 복구] ESO CRD를 잘못된 버전으로 먼저 적용했다가 재적용할 때 (2026-08-18 실측)

CRD를 한 번 잘못된 버전(예: 차트 targetRevision과 안 맞는 태그)으로
적용한 뒤 올바른 버전으로 다시 적용하면, Server-Side Apply가 새
매니페스트에 없는 필드(`spec.conversion` 등)를 옛 상태 그대로 유지하려
시도해 스키마 불일치 에러가 난다:
이 경우 영향받은 CRD만(실전에서는 `clustersecretstores`·`externalsecrets`·
`secretstores` 3종) 지우고 재적용하면 해결된다. `platform-secrets`가 아직
`Synced` 되기 전(=실제 사용 리소스가 없는 시점)이면 데이터 손실 없음:
```bash
kubectl delete crd externalsecrets.external-secrets.io secretstores.external-secrets.io clustersecretstores.external-secrets.io --ignore-not-found
kubectl apply --server-side -f <올바른 버전의 CRD 번들>
```
가장 안전한 예방은 처음부터 8-3 ①의 버전("targetRevision과 재대조")을
**실제로 재대조하고 시작하는 것** — 이번 실측은 재대조를 건너뛰고 다른
버전을 먼저 적용했다가 발생한 사례였다.

### 8-4. CUR 버킷 이관과 정의 재생성 (2026-08-13 신설)

destroy 게이트①에서 살림(keep)을 골랐다. `terraform state rm`은 버킷을 state에서만 제외하는 것이라 실제 버킷은 그대로 남아있다. `modules/storage/cur.tf`가 `random_id.cur_suffix`·`aws_s3_bucket.cur`를 조건 없이 선언하고 있어서, 다음 apply는 이 버킷을 state에서 찾지 못해 새 버킷을 하나 더 만든다. 그래서 옛 버킷 내용을 새 버킷으로 옮기고 옛 것을 지우는 절차가 필요하다.

처리 방식 확정 — (c)안. 새 버킷 생성을 허용하고 아래 절차로 마이그레이션한다(2026-08-13 확정).
(a) `terraform import`로 옛 버킷을 state에 복귀 — 근본 해결이나 `random_id` 임포트가 까다로워 기각.
(b) 코드에 조건부 플래그를 추가해 기존 버킷을 data source로만 참조 — 검증 안 된 코드를 넣는 것이라 기각.

확인 필요(2026-08-13): CUR 소급 여부 실험을 위해 8/13에 CLI로 `hailcast-dev-cur` 리포트 정의를 옛 버킷 대상으로 이미 만들어 놓았다. 아래 순서(새 버킷 생성 후 정의 재생성)를 그대로 따르면 같은 리포트 이름이 이미 존재해 `DuplicateReportNameException`이 날 수 있다. 진행 전에 `aws cur delete-report-definition --report-name hailcast-dev-cur --region us-east-1`로 기존 정의를 지운다.

순서(위 충돌 해결 후 적용):

```bash
# 1) 옛 버킷 파일을 새 버킷으로 복사 — 정의 재생성보다 먼저(안 그러면 AWS가 새로 쓰는
#    cost_and_usage_data_status·crawler-cfn.yml을 옛 파일이 나중에 덮어씀)
OLD_BUCKET=<옛 버킷 이름 — aws s3api list-buckets로 hailcast-dev-cur 접두어 조회>
CUR_BUCKET=$(terraform -chdir=envs/dev output -raw cur_bucket_name)
aws s3 sync "s3://$OLD_BUCKET/" "s3://$CUR_BUCKET/"
# 확인: 두 버킷 객체 수가 같아야 한다
aws s3 ls "s3://$OLD_BUCKET/" --recursive | wc -l
aws s3 ls "s3://$CUR_BUCKET/" --recursive | wc -l

# 2) 새 버킷 이름으로 CUR 정의 재생성 — 첫 배달까지 최대 24시간
aws cur put-report-definition --region us-east-1 --report-definition "{
  \"ReportName\": \"hailcast-dev-cur\", \"TimeUnit\": \"HOURLY\",
  \"Format\": \"Parquet\", \"Compression\": \"Parquet\",
  \"AdditionalSchemaElements\": [\"RESOURCES\"], \"S3Bucket\": \"$CUR_BUCKET\",
  \"S3Prefix\": \"cur\", \"S3Region\": \"ap-northeast-2\",
  \"AdditionalArtifacts\": [\"ATHENA\"], \"RefreshClosedReports\": true,
  \"ReportVersioning\": \"OVERWRITE_REPORT\"
}"

# 3) 객체 수 일치 확인 후 옛 버킷 삭제 — 안 지우면 다음 teardown의 CUR 가드가
#    버킷 이름 두 개를 받아 조용히 건너뛰어진다(teardown_체크리스트.md 10-1절 3중 가드 참고)
aws s3 rm "s3://$OLD_BUCKET/" --recursive
aws s3api delete-bucket --bucket "$OLD_BUCKET" --region ap-northeast-2
```

4) 규약서 5-9절의 CUR 버킷 이름(대상 버킷·CLI 예시의 `S3Bucket` 값)을 새 이름으로 갱신.

---

### 8-5. ArgoCD 설치 (2026-08-17 신설 — `make bootstrap-all` ①단계 상세)

> `make bootstrap-all`이 이 절차를 내부에서 실행한다. 아래는 그 단계의
> 상세 설명이며, ArgoCD만 따로 설치할 때는 `make install-argocd`를 쓴다.

destroy 시 ArgoCD는 클러스터와 함께 사라진다. GitOps 관리 '밖'의 부트스트랩이라
Application으로 자기 자신을 되살릴 수 없고, 사람이 1회 실행해야 한다.
`manifests scripts/install_argocd.sh`가 이 절차를 자동화한다(Helm 기반, 2026-07-31 팀 확정).

**전제조건 2가지** — 둘 다 없으면 스크립트가 즉시 중단한다.

| 조건 | 확인 | 없을 때 |
| --- | --- | --- |
| `helm` CLI | `helm version` | 2026-08-01에 새로 설치한 도구다. 기존 "필요한 도구"(`aws`·`terraform`·`kubectl`·`jq`)에 없었으니 새 작업 환경이면 먼저 설치: https://helm.sh/docs/intro/install/ |
| kubeconfig가 hailcast 클러스터를 가리킴 | `kubectl config current-context` | `aws eks update-kubeconfig --name hailcast-dev-eks --region ap-northeast-2` |

컨텍스트에 `hailcast` 문자열이 없으면 스크립트가 거부한다(로컬 kubeadm 등 엉뚱한
클러스터에 설치하는 사고 방지 — `teardown_manifest.sh`·`deploy.sh`와 같은 관례).

**실행**
```bash
make install-argocd          # ops 레포에서 (manifests로 위임)
# 또는 make -C ../project3-hailcast-manifests install-argocd
```

**이 명령이 하는 것 / 안 하는 것**

| 자동으로 됨 | 별도 필요 |
| --- | --- |
| argocd 네임스페이스 생성 | ESO CRD 설치 (→ 8-3 ①) |
| Helm 차트 `argo/argo-cd` 10.2.1 설치 (앱 v3.4.5) | 8-1의 배포팀 5줄 반영 |
| rollout 완료까지 대기 (`--wait`, 5분) | |
| app-of-apps(`hailcast-root`) 적용 | |
| 초기 admin 비밀번호 출력 | |

`helm upgrade --install`이라 멱등이다. 중간에 실패해 CRD만 남은 상태에서 다시 실행해도
안전하게 복구한다 — 실패하면 그냥 같은 명령을 다시 실행하면 된다.

**실행 후 확인**
```bash
kubectl -n argocd get deploy                      # argocd-server 등 전부 READY
kubectl -n argocd get application hailcast-root   # hailcast-root 존재
kubectl -n argocd get application                 # 하위 Application들이 생성되기 시작
```
`hailcast-root`가 잠시 OutOfSync/Progressing인 것은 정상이다 — 하위 Application을
sync-wave 순서대로 펼치는 중이다. 이 시점에는 8-1의 5줄이 아직 옛 값이라
Application 일부가 Degraded로 남는 것도 정상이며, 8-2의 4번 단계에서 해소된다.

**실패 시**

| 증상 | 원인 | 대처 |
| --- | --- | --- |
| `kubectl 컨텍스트가 'hailcast'를 포함하지 않습니다` | kubeconfig 미발급 | 위 전제조건 표의 `update-kubeconfig` |
| `helm CLI가 없습니다` | helm 미설치 | 위 전제조건 표 |
| `CRD가 여전히 없습니다` | Helm 설치 자체가 실패 | `helm -n argocd list`·`helm -n argocd status argocd`로 원인 확인 후 재실행 |
| 5분 타임아웃 | 노드 부족으로 파드 Pending | `kubectl -n argocd get pod`로 Pending 확인. 노드 Ready 후 재실행 |
| `initial-admin-secret 없음` | 비밀번호를 이미 변경했거나 삭제됨 | 설치 실패가 아니다. 스크립트가 안내하는 대체 명령 사용 |

**삭제(참고)**: `make teardown`의 C-1 단계가 담당한다. `argocd` CLI 로그인 없이
`kubectl patch`로 `hailcast-root`에 finalizer를 주입한 뒤 삭제한다.
finalizer가 root에만 주입되므로 애드온 7종(`grafana-dashboards`·`karpenter`·`keda`·
`kube-prometheus-stack`·`metrics-server`·`opencost`·`platform-monitoring`)은
Application만 지워지고 하위 자원이 남는다 — teardown_체크리스트.md 참고.

## 9. 확인 안 된 것 (재구축 쪽)

번호 1·6·7은 destroy 쪽이라 `teardown_체크리스트.md` §9에 있다. 아래는 재구축 쪽 2~5번이다.

| # | 항목 | 왜 모르나 |
| --- | --- | --- |
| 2 | `alb_dns_name`을 옛 값으로 둔 채 apply했을 때 Route53이 없어진 ELB DNS를 alias 대상으로 받아주는지 | 조회로 판별 불가(destroy 7-6·이 문서 8-2 순서로 이 경우 자체를 피하고 있음) |
| 3 | SQS 큐 삭제 직후 같은 이름으로 재생성 시 `QueueDeletedRecently`로 막히는지 | AWS 권장 대기 60초, 재구축을 바로 이어 하면 걸릴 가능성 |
| 4 | CUR 정의를 새 버킷 이름으로 재생성 시 콘솔이 버킷 정책을 덮는지 | 콘솔 경로 미시도 |
| 5 | 배포팀 계정에 `secretsmanager:GetSecretValue`·`ssm:GetParameter` 권한이 있는지 | 8-3 수동 백업 경로 실행 권한 미확인 — 거부되면 인프라 담당이 대신 실행 |

남은 결정 병목은 총 2개다. 성격이 둘로 갈린다.

"제3자가 이 문서만 보고 재구축" 가능 여부에 걸리는 병목 1개: 텔레그램 봇 토큰·채팅ID 보관처 — 값 자체가 인프라 문서 소관이 아니라 이 문서에 별도 절로 담지 않았다. 팀장 또는 모니터링 담당에게 요청한다. ~~ESO CRD 매니페스트 주소~~는 2026-08-13 해소(8-3절 — manifests targetRevision 대조로 직접 확인, 배포팀 답 불필요했음).

여기에 더해 성격이 다른 병목 1개가 있다. 8/13에 cur.tf 무조건 선언 문제가 새로 발견돼(8-4절) 다음 apply 자체가 막힐 수 있는 상태다. 이건 "제3자 재구축" 가능 여부와는 별개로 apply 전에 반드시 처리해야 하는 항목이다.

(원본 §9의 이 자리에 있던 "8/4 전 완성은 무리라는 게 이미선님 판단이고 팀장도 동의" 기록은 시점이 지나 뺐다. 결론(개선기간 과제로 이관)은 그대로 8/13 개선기간에서 진행 중이라는 뜻이다.)

---

*최초 분리 2026-08-13 — `teardown_체크리스트.md` 8~9장(재구축 관련)을 이 문서로 옮김. 원본 개정이력(7/28 대개정~7차 개정)은 그 문서에 있다.*
